import Hero from "../components/Hero";

const HomePage = () => {
    return (
        <div>
            <main>
                <Hero />
            </main>
        </div>

    );
};

export default HomePage;